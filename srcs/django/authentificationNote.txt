cd project/
python3.11 manage.py startapp members
add the application to tran_project/setting.py files
create a newFiles urls.py in the members directories
and copy this in it

`
	from django.urls import path
	from . import views

	urlpatterns = [
	]
`

add to tran_project/urls.py those 2 lines

path('members/', include('django.contrib.auth/urls')),
path('members/', include('members.urls'))

